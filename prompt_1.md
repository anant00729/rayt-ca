1. for each tab option selected use contrasting colors. I can see transitioning from "Photography" to "Jwelery"
the color changes is minimal i.e from #f0ece7 -> "#f0ece7". use from a range of RGB across all the contrasting color from the color pallet. the card color and the backgroud color must be contrasting and the card color and the text color, logo colors must be contrastting to the card colors. 
2. All the elements must have color schemes so contrasting that each one looks visible in the UI.
3. For icons use standard colors like coffee color is brown, leaf color is green. so decide color schema bases on the tab option name.
4. the tab selected color does not match with the website coloring use that colors defined in the website for the selected tabs just like the one shown in the pic attached.